% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_usa_map.R
\name{plot_usa_map}
\alias{plot_usa_map}
\title{Interactive USA Map (Highcharts)}
\usage{
plot_usa_map(
  data,
  values,
  by_x,
  by_y,
  map = "countries/us/us-all",
  decimals = 0,
  glimpse_data = FALSE,
  title = NULL
)
}
\arguments{
\item{data}{A data frame containing the values to map.}

\item{values}{A string specifying the column in `data` with numeric values
to display.}

\item{by_x}{A string specifying the column name in the Highcharts map data
to join on (e.g. `"hc-key"`).}

\item{by_y}{A string specifying the column name in `data` to join on.}

\item{map}{A string specifying the Highcharts map path. Default
`"countries/us/us-all"`. See
\url{https://code.highcharts.com/mapdata/} for available maps.}

\item{decimals}{Integer number of decimal places for tooltip values.
Default `0`.}

\item{glimpse_data}{Logical. If TRUE, downloads and returns the map data
for inspection without rendering the plot. Default `FALSE`.}

\item{title}{Optional string for the map title. Default `NULL`.}
}
\value{
A `highchart` map object, or (when `glimpse_data = TRUE`)
  invisibly returns the raw map data frame.
}
\description{
Generates an interactive Highcharts choropleth map for a US-level dataset.
Use `glimpse_data = TRUE` to inspect the map data and identify the correct
join column names before plotting.
}
\examples{
\dontrun{
df <- data.frame(
  code     = c("us-ca", "us-tx", "us-ny", "us-fl"),
  patients = c(120, 95, 80, 60)
)

# Inspect map join columns first
plot_usa_map(df, values = "patients", by_x = "hc-key", by_y = "code",
             glimpse_data = TRUE)

# Render map
plot_usa_map(df, values = "patients", by_x = "hc-key", by_y = "code",
             title = "Patients per State")
}
}
