% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_mean_change_table.R
\name{render_mean_change_table}
\alias{render_mean_change_table}
\title{Render Mean Change Summary Table}
\usage{
render_mean_change_table(
  data,
  header_color = "#1482FA",
  header_text_color = "#FFFFFF",
  header_font_size = 14,
  body_font_size = 12,
  fixed_header = TRUE,
  bootstrap_options = c("striped", "hover", "condensed"),
  full_width = TRUE,
  escape_html = FALSE
)
}
\arguments{
\item{data}{A data frame returned by [summarize_mean_change()], containing
at minimum: visit, n, raw_mean, raw_ci_lower, raw_ci_upper. Adjusted
columns (adjusted_mean, adj_ci_lower, adj_ci_upper) are included
automatically when present.}

\item{header_color}{Background colour for header cells. Default `"#1482FA"`.}

\item{header_text_color}{Text colour for header cells. Default `"#FFFFFF"`.}

\item{header_font_size}{Font size for header text in points. Default `14`.}

\item{body_font_size}{Font size for body text in points. Default `12`.}

\item{fixed_header}{Logical. If TRUE, the header remains fixed on scroll.
Default `TRUE`.}

\item{bootstrap_options}{A character vector of Bootstrap styling options.
Default `c("striped", "hover", "condensed")`.}

\item{full_width}{Logical. If TRUE, the table spans the full page width.
Default `TRUE`.}

\item{escape_html}{Logical. If TRUE, escapes HTML in cell content.
Default `FALSE`.}
}
\value{
A styled `kableExtra` HTML table.
}
\description{
Generates a styled HTML table using `kableExtra` to display the output of
[summarize_mean_change()]. Handles both raw-only and raw + adjusted outputs
automatically based on the columns present in `data`.
}
\examples{
\dontrun{
results <- summarize_mean_change(
  data         = df,
  patient_id   = "patient_id",
  visit        = "visit",
  score_change = "weight_change"
)

render_mean_change_table(results)
}
}
\seealso{
[summarize_mean_change()]
}
