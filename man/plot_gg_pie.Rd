% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gg_pie.R
\name{plot_gg_pie}
\alias{plot_gg_pie}
\title{Static pie chart (ggplot2)}
\usage{
plot_gg_pie(
  data,
  category,
  value,
  title = NULL,
  palette_type = c("diverging", "sequential"),
  palette = NULL,
  show_pct = TRUE,
  label_size = 4,
  label_threshold = 0.03,
  donut = FALSE,
  legend_position = "none"
)
}
\arguments{
\item{data}{A data frame containing the data to be plotted.}

\item{category}{A string specifying the column name for slice categories.}

\item{value}{A string specifying the column name for numeric slice values.}

\item{title}{A string for the plot title. Default NULL.}

\item{palette_type}{Palette type passed to `assign_palette()`: `"diverging"`
(default) or `"sequential"`. Ignored when `palette` is supplied.}

\item{palette}{A named character vector to override the auto palette
(e.g. `c("A" = "#1482FA", "B" = "#FF1F26")`). If NULL, colours are
assigned automatically via `assign_palette()`.}

\item{show_pct}{Logical. If TRUE (default), labels show category name and
percentage. If FALSE, labels show category name only.}

\item{label_size}{Numeric size of slice labels. Default `4`.}

\item{label_threshold}{Minimum slice fraction (0–1) required to display a
label. Suppresses labels on very small slices. Default `0.03`.}

\item{donut}{Logical. If TRUE, renders a donut chart. Default FALSE.}

\item{legend_position}{Legend position: `"none"` (default), `"right"`,
`"bottom"`, etc. Useful when there are many categories.}
}
\value{
A `ggplot` object.
}
\description{
Creates a pie or donut chart using ggplot2. Slice positions and label
coordinates are computed internally from `value`, so no pre-processing of
the data frame is required.
}
\examples{
df <- data.frame(
  fruit = c("Apple", "Banana", "Cherry", "Date"),
  count = c(10, 20, 30, 15)
)

# Standard pie chart
plot_gg_pie(df, category = "fruit", value = "count", title = "Fruit Counts")

# Sequential auto palette
plot_gg_pie(df, category = "fruit", value = "count", palette_type = "sequential")

# Manual palette override
plot_gg_pie(
  df,
  category = "fruit",
  value    = "count",
  palette  = c("Apple" = "#1482FA", "Banana" = "#FF1F26",
               "Cherry" = "#2ECC71", "Date" = "#F39C12")
)

# Donut chart with legend instead of labels
plot_gg_pie(
  df,
  category        = "fruit",
  value           = "count",
  donut           = TRUE,
  show_pct        = FALSE,
  legend_position = "right"
)
}
