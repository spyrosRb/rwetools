% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_incidence_rates.R
\name{calc_incidence_rates}
\alias{calc_incidence_rates}
\title{Calculate incidence rates with confidence intervals}
\usage{
calc_incidence_rates(
  data,
  event_var,
  person_time_var,
  group_vars = NULL,
  patient_id_var = NULL,
  person_time_unit = c("days", "years"),
  rate_scale = 1000,
  digits = 2
)
}
\arguments{
\item{data}{A data frame with one row per patient.}

\item{event_var}{A string. Column name of the binary event indicator (0/1).}

\item{person_time_var}{A string. Column name of the follow-up time variable.}

\item{group_vars}{A character vector of column names to stratify by.
Default `NULL` (no stratification).}

\item{patient_id_var}{A string. Column name of the patient identifier used
to count patients and compute the incidence proportion with an exact
binomial CI. Default `NULL` (proportion not computed).}

\item{person_time_unit}{Unit of `person_time_var`: `"days"` (converted to
years by dividing by 365.25) or `"years"`. Default `"days"`.}

\item{rate_scale}{Denominator for the incidence rate. Default `1000`
(rate per 1,000 person-years).}

\item{digits}{Number of decimal places for rounding. Default `2`.}
}
\value{

}
\description{
Summarises patient-level data into incidence rates per person-time with
exact Poisson (Byar's method) 95% confidence intervals, optionally
stratified by grouping variables. When a patient identifier column is
supplied, an incidence proportion with an exact binomial 95% CI is also
returned.
}
\examples{
\dontrun{
set.seed(42)
df <- data.frame(
  patient_id = 1:500,
  event      = rbinom(500, 1, 0.08),
  fup_days   = runif(500, 365, 1825),
  arm        = sample(c("Treatment", "Control"), 500, replace = TRUE)
)

# Overall
calc_incidence_rates(df,
  event_var      = "event",
  person_time_var = "fup_days",
  patient_id_var = "patient_id"
)

# By treatment arm, per 100,000 person-years
calc_incidence_rates(df,
  event_var       = "event",
  person_time_var = "fup_days",
  group_vars      = "arm",
  patient_id_var  = "patient_id",
  rate_scale      = 100000
)
}
}
