% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_vars.R
\name{summarise_vars}
\alias{summarise_vars}
\title{Summarise a Numeric Variable}
\usage{
summarise_vars(data, value, na.rm = TRUE)
}
\arguments{
\item{data}{A data frame (optionally grouped via [dplyr::group_by()]).}

\item{value}{A string specifying the numeric column to summarise.}

\item{na.rm}{Logical. If `TRUE` (default), `NA` values are excluded from
all statistics.}
}
\value{
A tibble with one row per group (or one row overall) and columns
  `n`, `missing`, `mean`, `sd`, `min`, `q25`, `median`, `q75`, `max`.
}
\description{
Computes descriptive statistics (n, missing, mean, SD, min, Q25, median,
Q75, max) for a numeric column. Works on plain and grouped data frames —
pass a grouped data frame to get per-group summaries.
}
\examples{
summarise_vars(mtcars, "mpg")
mtcars |> dplyr::group_by(cyl) |> summarise_vars("mpg")
}
