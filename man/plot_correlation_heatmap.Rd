% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_correlation_heatmap.R
\name{plot_correlation_heatmap}
\alias{plot_correlation_heatmap}
\title{Correlation Heatmap (Static or Interactive)}
\usage{
plot_correlation_heatmap(
  data,
  method = "pearson",
  focus_vars = NULL,
  title = "Correlation Heatmap",
  plot_method = "square",
  show_legend = TRUE,
  colors = c("#0b41cd", "#ffffff", "#FF1F26"),
  lab = TRUE,
  lab_col = "black",
  lab_size = 3,
  digits = 2,
  interactive = FALSE
)
}
\arguments{
\item{data}{A data frame containing numeric variables.}

\item{method}{Correlation method: `"pearson"` (default), `"kendall"`, or
`"spearman"`.}

\item{focus_vars}{Optional character vector of column names to subset before
computing correlation. Default `NULL`.}

\item{title}{A string for the plot title. Default `"Correlation Heatmap"`.}

\item{plot_method}{Shape for static plot cells: `"circle"` or `"square"`
(default). Static only.}

\item{show_legend}{Logical. Whether to show the legend. Default `TRUE`.
Static only.}

\item{colors}{A character vector of three colours for low, mid, and high
correlation. Default `c("#0b41cd", "#ffffff", "#FF1F26")`. Static only.}

\item{lab}{Logical. If TRUE, shows correlation values on cells. Default
`TRUE`. Static only.}

\item{lab_col}{Label colour. Default `"black"`. Static only.}

\item{lab_size}{Label size. Default `3`. Static only.}

\item{digits}{Number of decimal places to round correlation values. Default
`2`.}

\item{interactive}{Logical. If TRUE, returns an interactive `highcharter`
heatmap. Default `FALSE`.}
}
\value{
A `ggplot` or `highchart` object.
}
\description{
Computes a correlation matrix and plots a heatmap. Supports both a static
`ggcorrplot` visualization and an interactive `highcharter` visualization.
}
\examples{
\dontrun{
# Static heatmap
plot_correlation_heatmap(mtcars, method = "pearson")

# Interactive heatmap
plot_correlation_heatmap(mtcars, method = "spearman", interactive = TRUE)

# Focused variables
plot_correlation_heatmap(mtcars, focus_vars = c("mpg", "wt", "hp"))
}
}
