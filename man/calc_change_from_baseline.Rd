% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_change_from_baseline.R
\name{calc_change_from_baseline}
\alias{calc_change_from_baseline}
\title{Calculate Change from Baseline}
\usage{
calc_change_from_baseline(data, patient_id, visit, score, prefix = "")
}
\arguments{
\item{data}{A data frame containing longitudinal data.}

\item{patient_id}{A string specifying the column identifying each subject.}

\item{visit}{A string specifying the column identifying the visit or
time point (used to determine ordering).}

\item{score}{A string specifying the numeric column for which baseline and
change from baseline are calculated.}

\item{prefix}{An optional string prefix for the new column names. If
supplied, columns are named `<prefix>_baseline` and `<prefix>_change`;
otherwise `baseline` and `change`. Default `""`.}
}
\value{
The input data frame with two additional columns: baseline value
  and change from baseline. Rows where `visit` or `score` is `NA` receive
  `NA` for both new columns.
}
\description{
Derives a baseline value and change from baseline for a numeric score column
in longitudinal data. Baseline is defined as the first non-missing
observation ordered by `visit`. All original rows and columns are preserved;
two new columns are appended.
}
\examples{
\dontrun{
weight_cfb <- weight_data \%>\%
  calc_change_from_baseline(
    patient_id = "subject_id",
    visit      = "visit_dt",
    score      = "weight_kg",
    prefix     = "weight"
  )\%>\%
dplyr::filter(!is.na(change))
}
}
