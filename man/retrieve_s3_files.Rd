% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve_s3_files.R
\name{retrieve_s3_files}
\alias{retrieve_s3_files}
\title{Retrieve Files from an S3 Bucket}
\usage{
retrieve_s3_files(
  bucket_name,
  object_keys,
  target_path,
  local_path,
  file_type = "sas7bdat",
  con,
  verbose = TRUE
)
}
\arguments{
\item{bucket_name}{A string specifying the S3 bucket name.}

\item{object_keys}{A character vector of object keys (file names) to
download.}

\item{target_path}{A string specifying the path prefix within the bucket.}

\item{local_path}{A string specifying the local directory to save files
into. Must exist.}

\item{file_type}{A string specifying the default file extension to append
when an object key has none. Default `"sas7bdat"`.}

\item{con}{A connection object with a `download_file()` method.}

\item{verbose}{Logical. If `TRUE` (default), prints progress messages.}
}
\value{
`invisible(NULL)`. Called for its side effects.
}
\description{
Downloads one or more files from an S3 bucket to a local directory using
a connection object. If an object key already carries a file extension it
is used as-is; otherwise `file_type` is appended.
}
\examples{
\dontrun{
retrieve_s3_files(
  bucket_name = "my-bucket",
  target_path = "2024/data",
  object_keys = c("patients.sas7bdat", "visits"),
  local_path  = "./output",
  con         = s3
)
}
}
