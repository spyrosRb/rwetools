% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_comorbidities.R
\name{compute_comorbidities}
\alias{compute_comorbidities}
\title{Compute Comorbidity Scores for a Patient Cohort}
\usage{
compute_comorbidities(
  data,
  patient_id = "patient_id",
  diagnosis_code = "diagnosis_code",
  diagnosis_date = "diagnosis_date",
  index_date = "index_date",
  lookback_days = 365,
  followup_days = 0,
  icd = c("icd9", "icd10"),
  method = c("charlson", "elixhauser"),
  tidy.codes = TRUE
)
}
\arguments{
\item{data}{A data frame containing diagnosis data.}

\item{patient_id}{A string specifying the patient ID column. Default
`"patient_id"`.}

\item{diagnosis_code}{A string specifying the diagnosis code column. Default
`"diagnosis_code"`.}

\item{diagnosis_date}{A string specifying the diagnosis date column. Default
`"diagnosis_date"`.}

\item{index_date}{A string specifying the index date column. Default
`"index_date"`.}

\item{lookback_days}{Number of days before the index date to include
diagnoses. Default `365`.}

\item{followup_days}{Number of days after the index date to include
diagnoses. Default `0`.}

\item{icd}{A character vector: one or both of `"icd9"`, `"icd10"`. Default
`c("icd9", "icd10")`.}

\item{method}{Scoring method: `"charlson"` (default) or `"elixhauser"`.}

\item{tidy.codes}{Logical. If `TRUE` (default), ICD codes are uppercased and
non-alphanumeric characters removed before matching.}
}
\value{
A tibble with one row per patient containing comorbidity flags, raw
  score, weighted score, and index category (Charlson only).
}
\description{
Calculates Charlson or Elixhauser comorbidity scores from diagnosis data
using ICD-9 and/or ICD-10 codes via the \pkg{comorbidity} package.
When both ICD versions are supplied, per-patient flags are combined by
taking the maximum across versions before scoring.
}
\examples{
\dontrun{
compute_comorbidities(dx_data, method = "charlson")
compute_comorbidities(dx_data, icd = "icd10", method = "elixhauser")
}
}
