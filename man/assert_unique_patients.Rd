% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert_unique_patients.R
\name{assert_unique_patients}
\alias{assert_unique_patients}
\title{Assert One Row per Patient in a Database Table}
\usage{
assert_unique_patients(
  con,
  table_name,
  schema = NULL,
  patient_id = "patient_id"
)
}
\arguments{
\item{con}{A Redshift DBI connection object.}

\item{table_name}{A string specifying the table name.}

\item{schema}{An optional string specifying the schema name. Default `NULL`.}

\item{patient_id}{A string specifying the patient ID column name. Default
`"patient_id"`.}
}
\value{
`invisible(NULL)`. Called for its side effects.
}
\description{
Queries a Redshift table to verify that each patient has exactly one row,
by comparing total row count to the count of distinct patient IDs. Results
are reported to the console via `cli`.
}
\examples{
\dontrun{
assert_unique_patients(con, table_name = "patients", schema = "public")
}
}
