% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gg_upset.R
\name{plot_gg_upset}
\alias{plot_gg_upset}
\title{UpSet plot (ggplot2 / ComplexUpset)}
\usage{
plot_gg_upset(
  data,
  intersect,
  min_size = 0,
  n_intersections = NULL,
  color = "#1482FA",
  set_sizes = FALSE,
  show_total = TRUE,
  label_size = 3,
  width_ratio = 0.1,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{A data frame with binary (0/1 or TRUE/FALSE) group-membership
columns and optionally additional covariates.}

\item{intersect}{A character vector of column names that define set
membership (e.g. `c("weight_baseline", "weight_wk26", "weight_wk52")`).}

\item{min_size}{Minimum intersection size to display. Default `0`.}

\item{n_intersections}{Maximum number of intersections to show, ordered by
`sort_by`. Default `NULL` (all intersections).}

\item{color}{Fill colour for intersection bars. Default `"#1482FA"`.}

\item{set_sizes}{Logical. If TRUE, displays overall set-size bars on the
left. Default `FALSE`.}

\item{show_total}{Logical. If TRUE, annotates the intersection panel with
the total number of rows in `data`. Default `TRUE`.}

\item{label_size}{Size of the bar text labels (percentage + count). Default `3`.}

\item{width_ratio}{Ratio of set-matrix width to intersection bar width.
Default `0.1`.}

\item{title}{A string for the plot title. Default `NULL`.}

\item{subtitle}{A string for the plot subtitle. Default `NULL`.}
}
\value{
A `patchwork` object produced by `ComplexUpset::upset()`.
}
\description{
Creates an UpSet plot using the ComplexUpset package. The first argument
must be a data frame whose set-membership columns contain binary values
(0/1 or TRUE/FALSE); it may also contain covariate columns.
}
\examples{
\dontrun{
set.seed(42)
n <- 200
df <- data.frame(
  weight_baseline = sample(c(TRUE, FALSE), n, replace = TRUE, prob = c(0.9, 0.1)),
  weight_wk26     = sample(c(TRUE, FALSE), n, replace = TRUE, prob = c(0.7, 0.3)),
  weight_wk52     = sample(c(TRUE, FALSE), n, replace = TRUE, prob = c(0.5, 0.5))
)

plot_gg_upset(
  df,
  intersect = c("weight_baseline", "weight_wk26", "weight_wk52"),
  title     = "Weight Measurement Availability"
)
}
}
