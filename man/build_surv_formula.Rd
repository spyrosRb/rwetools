% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_surv_formula.R
\name{build_surv_formula}
\alias{build_surv_formula}
\title{Build a Survival Analysis Formula}
\usage{
build_surv_formula(time, status, covariates = NULL)
}
\arguments{
\item{time}{A string specifying the survival time variable name.}

\item{status}{A string specifying the event/censoring status variable name
(typically 0/1).}

\item{covariates}{An optional character vector of covariate names to include
on the right-hand side. Default `NULL` (intercept-only: `~ 1`).}
}
\value{
A `formula` object of the form `Surv(time, status) ~ covariates`.
}
\description{
Constructs a `Surv()` formula for use in Kaplan-Meier or Cox proportional
hazards models. When `covariates` is `NULL` a KM intercept-only formula is
returned; otherwise covariates are joined with `+` for use in Cox models.
}
\examples{
\dontrun{
# Kaplan-Meier (no covariates)
build_surv_formula(time = "os_time", status = "os_status")

# KM stratified by one variable
build_surv_formula(time = "os_time", status = "os_status",
                   covariates = "treatment")

# Cox model with multiple covariates
build_surv_formula(time = "os_time", status = "os_status",
                   covariates = c("age", "sex", "treatment"))
}
}
