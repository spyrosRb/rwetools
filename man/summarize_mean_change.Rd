% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_mean_change.R
\name{summarize_mean_change}
\alias{summarize_mean_change}
\title{Summarize Mean Change from Baseline by Visit}
\usage{
summarize_mean_change(
  data,
  patient_id,
  visit,
  score_change,
  adjustment_vars = NULL,
  use_adjusted_means = FALSE,
  conf_level = 0.95,
  na_rm = TRUE
)
}
\arguments{
\item{data}{A data frame containing the data.}

\item{patient_id}{A string specifying the column identifying each subject.}

\item{visit}{A string specifying the visit or time point column.}

\item{score_change}{A string specifying the column representing change from
baseline.}

\item{adjustment_vars}{A character vector of covariate column names to
adjust for in the linear model. Required when `use_adjusted_means = TRUE`.
Default `NULL`.}

\item{use_adjusted_means}{Logical. If TRUE, computes adjusted means via
`emmeans`. Default `FALSE`.}

\item{conf_level}{Confidence level for confidence intervals. Default `0.95`.}

\item{na_rm}{Logical. If TRUE, removes NAs when calculating means.
Default `TRUE`.}
}
\value{
A data frame with columns: visit, n, raw_mean, raw_ci_lower,
  raw_ci_upper, and (when `use_adjusted_means = TRUE`) adjusted_mean,
  adj_ci_lower, adj_ci_upper.
}
\description{
Calculates the mean change from baseline for a score variable by visit,
optionally computing adjusted means using the `emmeans` package. Visits
with fewer than 4 subjects trigger a warning.
}
\examples{
\dontrun{
summarize_mean_change(
  data         = df,
  patient_id   = "patient_id",
  visit        = "visit",
  score_change = "weight_change",
  conf_level   = 0.95
)

# With adjusted means
summarize_mean_change(
  data               = df,
  patient_id         = "patient_id",
  visit              = "visit",
  score_change       = "weight_change",
  adjustment_vars    = c("age", "sex"),
  use_adjusted_means = TRUE
)
}
}
