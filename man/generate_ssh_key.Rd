% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_ssh_key.R
\name{generate_ssh_key}
\alias{generate_ssh_key}
\title{Generate and Register an SSH Key}
\usage{
generate_ssh_key(key_type, email, ssh_path, platform)
}
\arguments{
\item{key_type}{A string specifying the SSH key type: `"ed25519"` or
`"rsa"`.}

\item{email}{A string specifying the email address to embed in the key
comment.}

\item{ssh_path}{A string specifying the full file path for the new key
(without `.pub` extension).}

\item{platform}{A string: `"GitHub"` or `"GitLab"`.}
}
\value{
`invisible(NULL)`. Called for its side effects.
}
\description{
Generates an SSH key pair of the specified type, adds the private key to
the SSH agent, prints the public key to the console, and opens the
platform's SSH key settings page in the default browser.
}
\examples{
\dontrun{
generate_ssh_key("ed25519", "user@example.com",
                 "~/.ssh/id_ed25519", "GitHub")
}
}
