% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_palette.R
\name{assign_palette}
\alias{assign_palette}
\title{Assign a colour palette based on number of categories}
\usage{
assign_palette(data, column, palette_type = c("diverging", "sequential"))
}
\arguments{
\item{data}{A data frame containing the categorical data.}

\item{column}{A string specifying the column name with categorical values.}

\item{palette_type}{Palette type: `"diverging"` (default) or `"sequential"`.}
}
\value{
A character vector of hex colour codes.
}
\description{
Assigns a colour palette to a categorical variable based on the number of
distinct categories. Supports two palette types: `"diverging"` (blue-orange)
and `"sequential"` (blue shades). For 1 or 2 categories specific palettes
are returned; for larger counts the closest available palette size is used.
}
\examples{
df <- data.frame(country = c("USA", "Canada", "Mexico", "Greece"))
assign_palette(df, "country", palette_type = "diverging")
assign_palette(df, "country", palette_type = "sequential")
}
