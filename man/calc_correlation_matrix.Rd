% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_correlation_matrix.R
\name{calc_correlation_matrix}
\alias{calc_correlation_matrix}
\title{Calculate Correlation Matrix}
\usage{
calc_correlation_matrix(
  data,
  method = "pearson",
  focus_vars = NULL,
  quiet = TRUE
)
}
\arguments{
\item{data}{A data frame containing numeric variables to correlate.}

\item{method}{Correlation method: `"pearson"` (default), `"spearman"`, or
`"kendall"`.}

\item{focus_vars}{Optional character vector of variable names to focus on
via `corrr::focus()`. Default `NULL`.}

\item{quiet}{Logical. If TRUE, suppresses messages during computation.
Default `TRUE`.}
}
\value{
A correlation tibble (`cor_df`) from `corrr::correlate()`,
  optionally filtered by `corrr::focus()`.
}
\description{
Computes a correlation matrix from a data frame using the specified method
(Pearson, Spearman, or Kendall) via `corrr::correlate()`. Optionally
reduces to a subset of variables using `corrr::focus()`.
}
\examples{
\dontrun{
# Full correlation matrix
calc_correlation_matrix(mtcars, method = "pearson")

# Focused on specific variables
calc_correlation_matrix(mtcars, method = "spearman",
                        focus_vars = c("mpg", "hp"))
}
}
